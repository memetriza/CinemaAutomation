@using System.Web.Optimization
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Sinema Bileti Admin</title>
    @Styles.Render("~/admin/styles")
</head>
<body>
    <div class="container">
        <ul class="nav nav-pills">
            <li>@Html.ActionLink("Filmler", "Index", "Movie", new { area = "admin" }, new { })</li>
            <li>@Html.ActionLink("Üyeler", "Index", "Users", new { area = "admin" }, new { })</li>
            <li class="pull-right">@Html.RouteLink("Anasayfaya Geri Dön", "Home")</li>
        </ul>
        <hr />
        @RenderBody()
    </div>
    @Scripts.Render("~/admin/scripts")
    @RenderSection("Scripts", false)
</body>
</html>